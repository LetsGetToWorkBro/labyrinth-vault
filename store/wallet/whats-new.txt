First TestFlight build.

The complete interface: send, receive, activity, the vault pairing screen, the swap flow, and the state that matters most, which is a returned transaction that does not match the one that was approved.

Bitcoin reads a real chain now, through an Esplora node you choose. There is no default node, because picking one would be picking who watches your addresses. Monero can reach a node and broadcast; scanning is not finished and the balance says so rather than showing a zero.
