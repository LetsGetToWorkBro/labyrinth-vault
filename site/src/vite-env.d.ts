/// <reference types="vite/client" />

// Vite turns an imported asset into a URL string and fingerprints the file
// with a hash of its contents. This reference is what tells TypeScript that,
// so `import poster from "./assets/hero-poster.webp"` type-checks instead of
// looking like a missing module.
